package com.github.tomasbjerre.wisp.ui

/** Compose semantics test tags — kept separate from display text so UI copy can change freely. */
object TestTags {
    const val HISTORY_ROW = "history_row"
    const val ROUTE_MAP = "route_map"
}
